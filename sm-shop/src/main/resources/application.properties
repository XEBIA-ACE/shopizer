# Cache TTL (in seconds) for the storeCurrencies Caffeine cache.
# Changing this value and restarting the application is sufficient to alter the TTL — no code change required.
# Default: 300 seconds (5 minutes)
store.currency.cache.ttl.seconds=300
